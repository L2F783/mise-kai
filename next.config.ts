import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Disable cache components for now (Next.js 16 experimental feature)
  // We can enable this later with proper Suspense boundaries
};

export default nextConfig;
